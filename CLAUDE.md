# bobohu

보육원 후원을 더 쉽게 하는 서비스. 모바일 앱이 메인이고 웹으로도 볼 수 있게 한다.
백엔드는 Spring(Java)으로 만든다.

이 프로젝트는 개발자의 **포트폴리오이자 학습 기록**이다. 코드와 함께 `docs/`의 지식 위키를 계속 쌓아 나간다.
위키는 Andrej Karpathy의 LLM Wiki 패턴을 따른다. 사람은 방향을 정하고 질문하며, 위키를 쓰고 정리하는 일은 LLM이 맡는다.

## 문서 구조

```
docs/
├── raw/               # 원본 자료 (기획 메모, 회의록, 외부 API 문서 등). 사람이 넣고 LLM은 읽기만 한다
├── wiki/              # LLM이 관리하는 지식 위키
│   ├── index.md       # 전체 페이지 목록 + 한 줄 요약. 항상 최신으로 유지
│   ├── log.md         # 위키 변경 기록 (시간순, 추가만 한다)
│   ├── overview.md    # 프로젝트 개요, 기술 스택, 주요 결정, 로드맵, 미정 사항
│   ├── versions.md    # 모든 기술의 버전, 호환 조건, 선택 이유
│   ├── architecture.md # 시스템 구성도, 서버 구조, 주요 흐름, 보안 아키텍처
│   ├── data-sources.md # 보육원 데이터 출처
│   ├── tech/          # 사용하는 기술 하나당 한 페이지 (개념, 핵심, 트레이드오프, 대안)
│   └── features/      # 기능 하나당 한 페이지 (요구사항, 동작 흐름, 구현 위치)
└── troubleshooting/   # 트러블슈팅 기록. 위키와 따로 관리한다
    └── README.md      # 트러블슈팅 목록
```

각 폴더의 `_template.md`가 페이지 양식이다. 새 페이지는 반드시 템플릿을 따른다.

## 작성 규칙

- 모든 문서는 한국어로 쓴다. 기술 용어는 처음 나올 때 원어를 괄호로 함께 적는다. 예: 의존성 주입(Dependency Injection)
- 파일 이름은 영어 kebab-case로 짓는다. 예: `spring-data-jpa.md`, `2026-09-25-cors-error.md`
- 페이지끼리는 GitHub에서도 동작하는 **상대 경로 마크다운 링크**로 연결한다. 예: `[Spring Boot](../tech/spring-boot.md)`
- 코드를 보면 알 수 있는 내용은 위키에 옮겨 적지 않는다. 위키에는 개념, 이유, 흐름, 주의점을 적는다. 구현 위치는 파일 경로로 가리킨다.
- 구조가 바뀌면(모듈, 외부 연동, 데이터 흐름, 보안 방식) `architecture.md`도 함께 고친다.
- 추측과 사실을 구분한다. 확인하지 않은 내용에는 `(확인 필요)`를 붙인다.

## 보안 원칙

이 서비스는 후원자, 시설 담당자, 아동의 정보를 다룬다. 보안을 최우선으로 한다. 자세한 내용은 `docs/wiki/architecture.md`의 보안 아키텍처를 따른다.
- 개인정보는 최소한으로 수집한다. **아동 개인정보는 받지 않는다.** 카드 정보는 저장하지 않는다.
- API 키, 비밀번호, 암호화 키 같은 비밀 정보를 코드나 git에 넣지 않는다.
- 로그에 개인정보를 남기지 않는다.
- 수정 API는 역할뿐 아니라 리소스 소유자인지도 확인한다.
- 새 기능을 설계할 때는 어떤 개인정보를 다루는지, 어떤 위협이 있는지 먼저 적는다.

## 작업 절차

### 1. 새 기술을 도입할 때 (Tech)

코드에 새 기술(라이브러리, 프레임워크, 인프라 등)이 처음 들어가면:
1. `docs/wiki/tech/_template.md`를 따라 `docs/wiki/tech/<기술명>.md`를 만든다.
2. **이 프로젝트에서 왜 선택했는지**와 **대안과 비교한 트레이드오프**를 반드시 채운다.
3. 그 기술을 사용하는 기능 페이지에서 링크를 건다.
4. `index.md`와 `log.md`를 갱신한다.

버전은 추측하지 말고 공식 문서에서 최신 버전과 호환 조건을 확인한 뒤 `versions.md`에 기록한다. 기술을 추가하거나 버전을 바꾸면 **왜 이 버전인지**(LTS 여부, 지원 기간, 다른 기술과의 호환 조건)를 함께 적는다. 면접에서 설명할 수 있어야 한다.

이미 페이지가 있는 기술을 더 깊게 쓰게 되면(예: JPA에서 연관관계 매핑을 처음 사용) 새 페이지를 만들지 말고 기존 페이지에 섹션을 추가한다.

### 2. 기능을 만들거나 바꿀 때 (Feature)

1. `docs/wiki/features/<기능명>.md`를 만들거나 갱신한다.
2. 사용한 기술 페이지와 관련 트러블슈팅을 링크한다.
3. `index.md`와 `log.md`를 갱신한다.

### 3. 문제를 해결했을 때 (Troubleshooting)

에러나 예상하지 못한 동작을 해결했으면:
1. `docs/troubleshooting/_template.md`를 따라 `docs/troubleshooting/YYYY-MM-DD-<요약>.md`를 만든다. 짧고 간단하게 쓴다.
2. `docs/troubleshooting/README.md` 목록 맨 위에 한 줄을 추가한다.
3. 관련 기술 페이지의 "관련 트러블슈팅" 섹션에 링크를 건다.
4. `log.md`를 갱신한다.

### 4. 원본 자료가 들어왔을 때 (Ingest)

`docs/raw/`에 새 자료가 들어오면 읽고, 관련된 overview, feature, tech 페이지를 모두 갱신한다. 새로 알게 된 내용이 기존 내용과 충돌하면 그 사실을 페이지에 명시한다.

### 5. 질문에 답할 때 (Query)

프로젝트 관련 질문을 받으면 `index.md`를 먼저 읽고 필요한 페이지로 들어간다. 답변이 다시 참고할 가치가 있으면 위키 페이지로 저장한다.

### 6. 점검 요청을 받았을 때 (Lint)

"위키 점검해줘"라는 요청을 받으면 다음을 확인하고 결과를 보고한 뒤 고친다.
- 위키 내용이 현재 코드와 맞지 않는 곳
- 코드에서 쓰는데 tech 페이지가 없는 기술
- `index.md`에 빠진 페이지, 어디서도 링크되지 않은 페이지
- 템플릿의 필수 섹션이 비어 있는 페이지

## log.md 형식

```
## [YYYY-MM-DD] <tech|feature|trouble|ingest|lint|setup> | <한 줄 요약>
- 변경된 파일 목록
```
