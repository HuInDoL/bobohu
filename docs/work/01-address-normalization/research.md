# 01. 주소 정리: 코드 리서치

> 단계: 1/4 코드 리서치 · 작성일: 2026-09-26
> 다음 단계: 구현 계획 (`plan.md`). 사용자가 시작하라고 할 때 진행한다.

## 1. 작업 목표
보건복지부 CSV의 주소 826개를 정리해서 다음 두 가지에 쓸 수 있게 만든다.
1. **네이버 Geocoding의 입력값**으로 써서 좌표를 얻는다 (작업 03).
2. `facility` 테이블의 `address`, `sido`, `sigungu` 컬럼을 채운다 (작업 02 CSV 가져오기에서 호출).

이 작업은 **주소 문자열을 정리하는 로직만** 다룬다. CSV를 읽거나 DB에 저장하는 일은 작업 02에서 한다.

## 2. 현재 코드 상태
| 항목 | 상태 | 위치 |
|---|---|---|
| 주소 컬럼 | `address_raw`(원본), `address`(정리본), `sido`, `sigungu` 준비됨 | `V1__create_facility.sql`, `Facility.java` |
| 엔티티 생성 | `Facility.fromPublicData(...)`가 정리된 값을 **인자로 받기만** 한다. 정리 로직은 없음 | `facility/domain/Facility.java` |
| 주소 정리 코드 | **없음** | |
| 상세주소(동·호·층) 컬럼 | **없음**. 필요하면 V2 마이그레이션으로 추가해야 한다 | |
| 테스트 기반 | JUnit 5 + AssertJ 사용 중. 매개변수화 테스트(`@ParameterizedTest`)는 `spring-boot-starter-test`에 포함된 `junit-jupiter-params`로 쓸 수 있다 | |

## 3. CSV 주소 분석 결과 (826건)

### 3.1 시·도 표기
| 문제 | 건수 | 예시 → 기대값 |
|---|---|---|
| 줄임 표기 | 다수 | `서울시`, `서울` → `서울특별시` / `부산시` → `부산광역시` / `인천` → `인천광역시` / `충북` → `충청북도` / `충남` → `충청남도` / `경북` → `경상북도` |
| 표기가 섞임 | 울산 11 | `울산시` 6, `울산광역시` 4, `울산` 1 |
| **시·도 없이 시로 시작** | 19 | `천안시`(12), `당진시`(3) → 충청남도 / `용인시`(2), `안성시`(1) → 경기도 / `세종시`(1) → 세종특별자치시 |
| **행정구역 명칭 변경** (CSV는 2022년 기준) | 강원 27, 전북 58 | `강원도` → `강원특별자치도` (2023-06-11) / `전라북도`, `전북` → `전북특별자치도` (2024-01-18) |
| **관할 변경** | 1 | `경북 군위군` → `대구광역시 군위군` (2023-07-01 편입) |

### 3.2 시·군·구 표기
| 문제 | 예시 |
|---|---|
| **"시"를 뺀 줄임** (경상북도 28건 중 대부분) | `경상북도 포항 ...`, `경산`, `김천`, `청도`, `의성`, `영덕`, `성주` → `포항시`, `청도군` 등으로 복원해야 한다 |
| 중복 표기 | 8건. `제주시 제주시`, `동작구 동작구`, `횡성군 횡성군`, `천안시 동남구 동남구`, `창원시 창원시`, `세종시 세종시` |
| 앞부분 전체 중복 | `서울시 영등포구 서울 영등포구 여의대방로 65 ...` |
| 일반구가 있는 시 | `천안시 동남구`, `용인시 수지구`, `창원시 의창구` 등. `sigungu`를 `천안시 동남구`처럼 저장할지 정해야 한다 |

### 3.3 도로명과 건물번호
| 문제 | 건수 | 예시 → 기대값 |
|---|---|---|
| 도로명과 번호가 붙음 | 약 190 | `용마산로271` → `용마산로 271` / `양지5길36` → `양지5길 36` / `동소문로15길99` → `동소문로15길 99` |
| **도로명 안에 공백** (반대 방향 문제) | 여러 건 | `상도로 39길 48` → `상도로39길 48` / `터미널 9길 59` → `터미널9길 59` / `한글비석로 49길20` → `한글비석로49길 20` / `올림픽로 47길` |
| 읍·면과 도로명이 붙음 | 2 | `합덕읍버그내1길193-11` → `합덕읍 버그내1길 193-11` / `대정읍신영로36번길6` |
| 지번 주소로 보이는 것 | 1 | `경기도 시흥시 사미실90 송암동산` |

### 3.4 상세주소와 부가 정보
| 종류 | 건수 | 예시 |
|---|---|---|
| **동·호·층 정보** | 404 (약 49%) | `103동 604호`, `3층`, `122동1002호`, `201호,202호`, `1,2층` |
| 괄호 참고항목 | 92 | `(사직동)`, `(만덕동,럭키아파트)`, `(청수동, 극동아파트)` |
| 건물명이나 시설명 꼬리 | 여러 건 | `... 239 영명보육원`, `... 15 동명아동복지센터`, `... 20-11 초록꿈터` |
| 쉼표 | 54 | 주로 상세주소(`301, 302호`)와 참고항목 안 |
| 앞뒤 공백, 연속 공백 | 291 | `... 149 `, `... 11  ` |

- 같은 주소를 쓰는 시설: 11곳 (예: 꿈나무마을의 초록꿈터와 연두꿈터)
- 가장 긴 주소: 51자 (컬럼 길이 300자 안에 충분히 들어감)

## 4. 외부 자료 조사

### 4.1 네이버 Geocoding API (확인함)
- **요청**: `GET https://maps.apigw.ntruss.com/map-geocode/v2/geocode?query={주소}`
- **인증 헤더**: `x-ncp-apigw-api-key-id`(Client ID), `x-ncp-apigw-api-key`(Client Secret)
- **응답의 주요 필드**
  - `roadAddress`, `jibunAddress`: 표준화된 도로명주소와 지번주소
  - `addressElements`: `SIDO`, `SIGUGUN`, `DONGMYUN`, `RI`, `ROAD_NAME`, `BUILDING_NUMBER`, `BUILDING_NAME`, `POSTAL_CODE` 등으로 나뉜 주소 구성요소
  - `x`(경도), `y`(위도)
- 상세주소(동·호·층)를 어떻게 처리하는지는 공식 문서에 설명이 없다 (**확인 필요**, 작업 03에서 실제 호출로 확인).

### 4.2 행정안전부 도로명주소 검색 API (대안)
- `business.juso.go.kr`에서 승인키(`confmKey`)를 받아 쓰는 공공 API다. 응답에 시·도(`siNm`), 시·군·구(`sggNm`), 도로명주소(`roadAddrPart1`) 등이 나뉘어 온다.
- 주소 표준화 자체는 가장 정확하지만 **좌표는 주지 않는다**(**확인 필요**). 승인 절차, 이용 조건, 호출 한도는 이번에 확인하지 못했다 (**확인 필요**).

### 4.3 행정구역 변경 (확인함)
- 2023-06-11 강원도 → 강원특별자치도
- 2023-07-01 경상북도 군위군 → 대구광역시 군위군
- 2024-01-18 전라북도 → 전북특별자치도

## 5. 설계에 영향을 주는 발견

### 5.1 표준 주소는 Geocoding 응답에서 얻을 수 있다
Geocoding 응답의 `roadAddress`와 `addressElements`(SIDO, SIGUGUN)는 이미 표준화된 값이다. 그래서 두 가지 방향이 가능하다.
- **A. 직접 규칙으로 완벽하게 정리**: 위 3장의 모든 문제를 코드 규칙으로 해결한다. 외부 API 없이도 `sido`, `sigungu`가 채워진다. 대신 규칙이 많고 예외가 계속 나온다.
- **B. Geocoding이 성공할 만큼만 정리**: 명백한 오류(공백, 중복, 붙은 번호, 상세주소 분리)만 규칙으로 고치고, 표준 주소와 `sido`, `sigungu`는 Geocoding 응답으로 확정한다. 규칙이 적지만, Geocoding 전까지는 `sido`, `sigungu`가 비어 있거나 임시값이다.

### 5.2 상세주소를 분리해야 한다
- 동·호·층, 괄호 참고항목, 건물명 꼬리를 Geocoding 입력에 그대로 넣으면 검색 실패의 원인이 될 수 있다 (**추측**, 작업 03에서 확인).
- 분리한 상세주소를 **저장할지**, 저장한다면 **어디에**(새 컬럼 `address_detail`) 둘지 정해야 한다.

### 5.3 개인정보와 안전: 그룹홈 주소의 호수
- 호수까지 포함된 주소 중 **그룹홈이나 가정형 시설로 보이는 곳이 156건**이다. 대부분 일반 아파트나 빌라의 한 세대다.
- 이 주소를 지도 앱에서 호수까지 공개하면 **아이들이 사는 집의 정확한 위치**가 드러난다. 공공데이터에 있는 정보라도, 앱에서 찾기 쉽게 보여주면 노출 위험이 커진다. [아키텍처의 보안 원칙](../../wiki/architecture.md#6-보안-아키텍처)과 [아동 개인정보 원칙](../../wiki/features/facility-management.md#개인정보-원칙)에 비추어 볼 문제다.
- 주소 정리 단계에서 상세주소를 분리해 두면, 나중에 **공개 범위를 유형별로 다르게** 할 수 있다(예: 그룹홈은 상세주소 비공개, 지도 마커는 대략적인 위치만).

### 5.4 정리 규칙은 계속 바뀐다
원본(`address_raw`)을 보존하고 있으므로, 규칙을 고친 뒤 다시 처리할 수 있다. 정리 로직은 **입력 문자열만 받아 결과를 돌려주는 순수 함수**로 만들면 DB 없이 빠르게 테스트할 수 있다.

## 6. 구현 계획에서 정해야 할 것
| # | 결정 사항 | 선택지 |
|---|---|---|
| 1 | 정리 수준 | A. 규칙으로 완벽하게 / B. Geocoding이 성공할 만큼만 (5.1) |
| 2 | 상세주소 | 버림 / 새 컬럼에 저장 (5.2) |
| 3 | 그룹홈 등 가정형 시설의 주소 공개 범위 | 이번 작업에서는 분리만 하고 공개 정책은 지도 API 작업(04)에서 결정 / 지금 결정 (5.3) |
| 4 | 행정구역 명칭 | 현재 이름으로 바꿈(강원특별자치도 등) / CSV 원래 이름 유지 |
| 5 | `sigungu` 표기 | `천안시 동남구`(시+구) / `동남구`(구만) |
| 6 | 도로명주소 API 사용 | 사용 안 함(네이버만) / 정리 검증용으로 추가 (승인 절차 확인 필요) |

## 7. 확인한 사실과 추측
- **확인함**: CSV 분석 수치(직접 실행), 네이버 Geocoding의 요청과 응답 형식(공식 문서), 행정구역 변경 날짜(지자체 발표와 보도)
- **추측**: 상세주소가 Geocoding 실패를 일으킨다는 점, 도로명주소 API가 좌표를 주지 않는다는 점
- **미확인**: 도로명주소 API 승인 절차와 이용 조건, 네이버 Geocoding의 호출 한도와 요금

## 참고 자료
- [NAVER Cloud Maps Geocoding API 문서](https://api.ncloud-docs.com/docs/application-maps-geocoding)
- [주소기반산업지원서비스 (도로명주소 API)](https://business.juso.go.kr/)
- [강원특별자치도 출범 (강원도민일보)](https://www.kado.net/news/articleView.html?idxno=1187659)
- [전북특별자치도 출범으로 행정구역 명칭 변경 (전북특별자치도청)](https://www.jeonbuk.go.kr/newsroom/board/view.jeonbuk?boardId=BBS_0000090&menuCd=DOM_000001101000000000&paging=ok&startPage=1&dataSid=535135)
- [군위군의 대구광역시 편입 (위키백과)](https://ko.wikipedia.org/wiki/%EA%B5%B0%EC%9C%84%EA%B5%B0%EC%9D%98_%EB%8C%80%EA%B5%AC%EA%B4%91%EC%97%AD%EC%8B%9C_%ED%8E%B8%EC%9E%85)
